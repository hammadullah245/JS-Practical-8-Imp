//                     random number between 1 and 100
// var randomNumber = Math.floor(Math.random() * 100) + 1;
// console.log(randomNumber);
// Ask user for the minimum number




//                    random number between two user input
// var minNumber = prompt("Enter the minimum number:");
// minNumber = parseFloat(minNumber);

// var maxNumber = prompt("Enter the maximum number:");
// maxNumber = parseFloat(maxNumber);

// var randomNumber = Math.floor(Math.random() * (maxNumber - minNumber + 1)) + minNumber;

// alert("Random number between " + minNumber + " and " + maxNumber + ": " + randomNumber);



//               Simulate a coin toss by generating a random "Heads" or "Tails" accordingly.
// var coinToss = Math.floor(Math.random() * 2);

// if (coinToss === 0) {
//     console.log("Heads");
// } else {
//     console.log("Tails");
// }

